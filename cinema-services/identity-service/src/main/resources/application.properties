spring.config.import=optional:file:.env[.properties],\
  optional:file:MSS301-Backend-dev/cinema-services/identity-service/.env[.properties],\
  optional:file:cinema-services/identity-service/.env[.properties],\
  optional:file:services/identity-service/.env[.properties],\
  optional:file:identity-service/.env[.properties]

spring.application.name=${APPLICATION_NAME:identity-service}

server.port=${SERVER_PORT:8081}
server.address=${SERVER_ADDRESS:127.0.0.1}

# Database Configuration (Port 5431 or Neon cloud)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5431/identity_db}
spring.datasource.username=${DB_USERNAME:identity_user}
spring.datasource.password=${DB_PASSWORD:identity_pass_123}
spring.datasource.driver-class-name=${DB_DRIVER:org.postgresql.Driver}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.open-in-view=${JPA_OPEN_IN_VIEW:false}
spring.jpa.properties.hibernate.format_sql=true

# Flyway Migration
spring.flyway.enabled=${FLYWAY_ENABLED:true}
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=${FLYWAY_LOCATIONS:classpath:db/migration}

# JWT Security
app.jwt.secret=${JWT_SECRET:cineai-development-secret-key-please-change-123456}
app.jwt.access-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:1800000}
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

# Mail / SMTP Configuration
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:iwakeupwhen@gmail.com}
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:iwakeupwhen@gmail.com}
spring.mail.password=${MAIL_PASSWORD:dghy zgeq vuov yyom}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS_ENABLE:true}

# Google OAuth
app.google.client-id=${GOOGLE_OAUTH_CLIENT_ID:316168245134-0oda35k7fpifvgq2fgv0k66ifuq0omko.apps.googleusercontent.com}
app.google.token-info-base-url=${GOOGLE_TOKEN_INFO_BASE_URL:https://oauth2.googleapis.com}

# Seed Accounts
app.seeders.account.admin.email=${SEED_ADMIN_EMAIL:admin@cinemaai.com}
app.seeders.account.admin.password=${SEED_ADMIN_PASSWORD:Admin123}
app.seeders.account.admin.full-name=${SEED_ADMIN_FULL_NAME:CinemaAI Admin}
app.seeders.account.admin.phone=${SEED_ADMIN_PHONE:0900000001}

app.seeders.account.staff.email=${SEED_STAFF_EMAIL:staff@cinemaai.com}
app.seeders.account.staff.password=${SEED_STAFF_PASSWORD:Staff123}
app.seeders.account.staff.full-name=${SEED_STAFF_FULL_NAME:CinemaAI Staff}
app.seeders.account.staff.phone=${SEED_STAFF_PHONE:0900000002}

# Gateway Security (Zero-trust: only API Gateway is allowed)
app.gateway.secret=${INTERNAL_GATEWAY_SECRET:8F78D52690EED1A48867F89272F07391B8FBC8968F187BB5C53C60E20243D7AD}
app.internal.secret=${INTERNAL_SERVICE_SECRET:CF419427F61EE9D8880297E0309BDFB4504B8917C10B7019A56B1562344DDA03}
logging.pattern.level=${LOG_LEVEL_PATTERN:%5p}

# Actuator
management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS:health,info}
management.endpoint.health.show-details=${HEALTH_SHOW_DETAILS:never}

# SpringDoc OpenAPI (Swagger UI tắt trên cổng con 8081, chỉ bật api-docs để Gateway đọc)
springdoc.api-docs.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.enabled=false

